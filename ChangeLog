2026-05-20  Kamila Szewczyk  <k@iczelia.net>

	add the NIST 32x32 binary matrix-rank test

	add Bandt-Pompe permutation entropy

	add the windowed Maurer universal test

	add scalar and batched SIMD Berlekamp-Massey scorers

	group extended tests into measured -e, -ee, and -eee bands

	add output fields, plots, annotated verdicts, and recursive sort keys for
	the new estimators

	keep grid reductions stable across I/O modes and thread counts

2026-05-16  Kamila Szewczyk  <k@iczelia.net>

	add order-1 entropy, mutual information, runs, longest run, and bit cusum

	add min-entropy, collision entropy, index of coincidence, poker, variance,
	redundancy, symbol summaries, and bit-position bias

	add annotated PASS, WEAK, and FAIL reports

	add FIPS 140-2 monobit, poker, runs, and long-run tests

	generalize the chi-square upper tail to odd degrees of freedom

	factor the deterministic, libm-free logarithm routines

	vendor yarg and simplify command-line parsing

	add JSON aliases and explicit color and I/O modes

	align histogram axes and keep ordinary output within 80 columns

2026-05-15  Kamila Szewczyk  <k@iczelia.net>

	add AArch64 and ARMv7 NEON kernels

	add AArch64 SVE2 and AVX-512 BITALG kernels

	add WebAssembly SIMD128 kernels

	add io_uring on Linux and IOCP on Windows

	add terminal histograms and recursive CSV or JSON output

	separate CPU, I/O, OS, terminal, thread, and directory-walk ports

	add native Windows threads, UTF-8 paths, and UTF-8 console output

	add Windows 95, DJGPP/MS-DOS, WebAssembly, and further Linux targets

	replace libm entropy with stable double-double evaluation

	make variant dispatch table-driven

2026-05-15  Kamila Szewczyk  <k@iczelia.net>

	initial release

	report entropy, chi-square, mean, Monte Carlo pi, and serial correlation

	add byte and bit modes with fused case folding

	add plain, CSV, and JSON output with optional occurrence counts

	add scalar, SSSE3, SSE4.1, AVX2, and AVX-512 kernels

	add mapped and streamed input

	add deterministic pthread reduction

	add the Autotools build, test suite, benchmarks, and release targets
